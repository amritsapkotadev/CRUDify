<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/public/images/stylesheet/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex flex-col items-center justify-center min-h-screen">
  <div class="nav mb-4"> 
    <a href="/read" class="text-blue-500 hover:text-blue-700">Read Users</a>
  </div>
  
  <div class="bg-white p-8 rounded-lg shadow-lg max-w-md w-full">
    <!-- Heading -->
    <h1 class="text-2xl font-bold text-center text-gray-700 mb-6">Update User</h1>
    
    <!-- Form -->
    <form action="/update/<%= user._id %>" method="post" class="space-y-6">
      <!-- Name -->
      <div>
        <input 
          class="w-full px-4 py-2 border border-gray-300 rounded-lg text-gray-700 focus:outline-none focus:border-blue-500 transition duration-200" 
          type="text" 
          name="name" 
          placeholder="Name" 
          value="<%= user.name %>"  
          required>
      </div>
      <!-- Email -->
      <div>
        <input 
          class="w-full px-4 py-2 border border-gray-300 rounded-lg text-gray-700 focus:outline-none focus:border-blue-500 transition duration-200" 
          type="email" 
          name="email" 
          value="<%= user.email %>" 
          placeholder="Email" 
          required>
      </div>
      <!-- Password -->
      <div>
        <input 
          class="w-full px-4 py-2 border border-gray-300 rounded-lg text-gray-700 focus:outline-none focus:border-blue-500 transition duration-200" 
          type="password" 
          name="password" 
          value="<%= user.password %>" 
          placeholder="Password" 
          required>
      </div>
      <!-- Image URL -->
      <div>
        <input 
          class="w-full px-4 py-2 border border-gray-300 rounded-lg text-gray-700 focus:outline-none focus:border-blue-500 transition duration-200" 
          type="url" 
          name="imageUrl" 
          value="<%= user.image %>"  
          placeholder="Image URL" 
          required>
      </div>
      <!-- Submit Button -->
      <div>
        <button 
          type="submit" 
          class="w-full bg-blue-500 text-white py-2 px-4 rounded-lg hover:bg-yellow-600 transition duration-200">
          Update user
        </button>
      </div>
    </form>
  </div>
</body>
</html>
